/**
 * @file 详情页
 * @author saber team
 */

define(function (require) {

    var config = {};

    config.model = require('./indexModel');

    config.view = require('./indexView');

    return config;

});
