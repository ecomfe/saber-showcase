/**
 * @file new list view
 * @author saber team
 */

define(function (require) {

    var config = {};

    config.constructor = require('./View');

    config.template = require('./new.tpl');

    return config;

});
