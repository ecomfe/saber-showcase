/**
 * @file host list view
 * @author saber team
 */

define(function (require) {

    var config = {};

    config.constructor = require('./View');

    config.template = require('./hot.tpl');

    return config;

});
